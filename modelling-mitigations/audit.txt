Agent(Thorough data quality audit for AlgoBet ML features)
  ⎿  Prompt:
       You are doing a thorough data quality audit for an AlgoBet ML football prediction platform. The goal is to verify that every feature the ML models expect is being generated correctly, with appropriate
       coverage, value ranges, and no silent failures.

       Working directory: /home/arch/Coding/algobet

       Context

       The platform trains XGBoost/LightGBM models and a MarketMediationPredictor on football match data. Features are generated by FeaturePipeline from SQLAlchemy match objects, then used for training and
       inference. The recent debugging session revealed that market_mediation features were being silently dropped (17 instead of 205 features). Now we want a full audit.

       What to audit

       1. Feature generators — what they produce

       Read and summarize each generator in algobet/predictions/features/:
       - What raw columns each generator reads from the match DataFrame
       - What feature names each produces
       - What default/fallback values are used when data is missing
       - Any conditions under which a feature silently becomes 0 or NaN

       Key files:
       - algobet/predictions/features/generators.py (or similar — list files first)
       - algobet/predictions/features/composite.py
       - algobet/predictions/features/market_mediation_generator.py
       - algobet/predictions/features/pipeline.py

       2. Database state — actual data coverage

       Run queries in the Docker container. The API container is algobet-api. Run via:
       docker exec algobet-api python3 -c "<code>"

       Check the following. Use SQLAlchemy or raw psycopg2. The DB URL is in env var DATABASE_URL or can be constructed from POSTGRES_* vars. Try:
       import os
       db_url = os.environ.get('DATABASE_URL') or
       f"postgresql://{os.environ['POSTGRES_USER']}:{os.environ['POSTGRES_PASSWORD']}@{os.environ['POSTGRES_HOST']}:{os.environ['POSTGRES_PORT']}/{os.environ['POSTGRES_DB']}"

       Match table coverage:
       - Total matches
       - Matches with home_score/away_score (completed)
       - Matches with odds_home/draw/away (legacy odds)
       - Matches with opening_odds_{home,draw,away}
       - Matches with closing_odds_{home,draw,away}
       - Matches with avg/max odds columns
       - Matches with AH odds (odds_asian_handicap)
       - Matches with OU odds (odds_over_under_25)
       - Matches with opening_avg, opening_max, closing_avg, closing_max odds
       - Date range of matches
       - Breakdown by season (season_id) — how many matches per season

       Match statistics coverage:
       - Table: match_statistics (or similar)
       - Fields: xg_home, xg_away, ppda_*, shots_*, corners_*
       - Coverage rate

       Opening vs closing odds validity:
       - Matches where opening_odds_home IS NOT NULL AND closing_odds_home IS NOT NULL
       - Of those: matches where opening != closing (real movement)
       - Distribution of CLV = opening/closing - 1 (home): min, max, mean, std, % positive
       - Same for draw and away

       Model feature table:
       - Table: model_features or similar
       - Schema version distribution
       - Feature name counts

       3. Feature generator walkthrough

       For the market_mediation_generator specifically:
       - What column names does it look for? (e.g., opening_odds_home vs odds_home)
       - What fallback logic exists?
       - What gets set to 0.0 vs NaN when data is missing?
       - List ALL 25 feature names it generates

       For the main feature generators (form, standings, elo, etc.):
       - What lookback windows are used (last N matches)?
       - Are there minimum data requirements that cause silent 0s?
       - What happens when a team has fewer than N historical matches?

       4. Feature pipeline — what columns are passed to generators

       Read algobet/predictions/features/pipeline.py:
       - The prepare_match_dataframe function — what columns does it include from ORM Match objects?
       - Cross-reference with Match model (algobet/matches/models.py) — do all referenced columns exist on the model?

       5. Feature-to-model expectations

       For the trained model (model_id 243, version market_mediation_20260518_042022):
       - Read /app/data/models/market_mediation/market_mediation_20260518_042022/feature_pipeline/config.json from within the container
       - What 205 feature names does it expect?
       - Cross-reference against what the generators actually produce — are all expected names actually generated?

       6. Variance and zero-rate checks

       Run a quick feature distribution audit by loading actual training data:

       # Load a recent model's feature pipeline
       from algobet.predictions.features.pipeline import FeaturePipeline
       pipeline = FeaturePipeline.load('/app/data/models/market_mediation/market_mediation_20260518_042022/feature_pipeline')
       print(pipeline.feature_names[:10])

       If possible, spot-check the actual feature matrix for:
       - Features with near-zero variance (constant features that add no signal)
       - Features with very high null/zero rate (>80%)
       - Features with extreme values (potential normalization failures)

       7. Match model column completeness

       Read algobet/matches/models.py and check:
       - Which columns are nullable vs NOT NULL
       - Whether opening_avg/max odds fields exist (they were added via migration 9f6b4c2d8a1e_add_opening_closing_odds_to_matches.py)
       - Whether there's a discrepancy between what the migration added and what prepare_match_dataframe accesses

       Check the migration file at algobet/versions/9f6b4c2d8a1e_add_opening_closing_odds_to_matches.py (or wherever it is).

       Output format

       Produce a structured report with these sections:
       1. Feature generator inventory — table of generator → feature names produced
       2. DB coverage summary — table of column → count and % coverage
       3. Opening vs closing odds analysis — CLV distribution stats
       4. Market mediation features — detailed per-feature coverage check
       5. Model expectation vs generation — any mismatches between what the 205-feature model expects and what generators produce
       6. Silent failure modes — list of any conditions where a feature silently becomes 0 or produces misleading values
       7. Recommendations — specific fixes or data gaps to address

       Be specific with numbers. This is a quality audit, so precision matters. Run actual database queries to get real counts, not estimates.

     Bash(ls /home/arch/Coding/algobet/algobet/predictions/features/)
     Bash(ls /home/arch/Coding/algobet/alembic/versions/ | grep -i odds)
     Bash(docker ps --format "{{.Names}}" | grep algobet)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/generators.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/market_mediation_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/pipeline.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/composite.py)
     Read(/home/arch/Coding/algobet/algobet/matches/models.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/team_form_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/odds_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/detailed_odds_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/head_to_head_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/standings_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/enriched_stats_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/temporal_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/elo_rating_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/draw_signal_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/matchup_interaction_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/expected_points_generator.py)
     Read(/home/arch/Coding/algobet/algobet/predictions/features/odds_residual_generator.py)
     Bash(docker exec algobet-api python3 -c "
         import os
         db_url = os.environ.get('DATABASE_URL') or
         f\"postgresql://{os.environ['POSTGRES_USER']}:{os.environ['POSTGRES_PASSWORD']}@{os.environ['POSTGRES_HOST']}:{os.environ['POSTGRES_PORT']}/{os.environ['POSTGRES_DB']}\"
         print('DB URL (masked):', db_url.split('@')[1])

         import psycopg2
         conn = psycopg2.connect(db_url)
         cur = conn.cursor()

         cur.execute('SELECT COUNT(*) FROM matches')
         total = cur.fetchone()[0]
         print(f'Total matches: {total}')

         cur.execute('SELECT COUNT(*) FROM matches WHERE home_score IS NOT NULL AND away_score IS NOT NULL')
         completed = cur.fetchone()[0]
         print(f'Completed (with scores): {completed} ({100*completed/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE odds_home IS NOT NULL')
         odds = cur.fetchone()[0]
         print(f'With odds_home: {odds} ({100*odds/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE opening_odds_home IS NOT NULL')
         open_odds = cur.fetchone()[0]
         print(f'With opening_odds_home: {open_odds} ({100*open_odds/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE closing_odds_home IS NOT NULL')
         close_odds = cur.fetchone()[0]
         print(f'With closing_odds_home: {close_odds} ({100*close_odds/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE avg_home_odds IS NOT NULL')
         avg_odds = cur.fetchone()[0]
         print(f'With avg_home_odds: {avg_odds} ({100*avg_odds/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE max_home_odds IS NOT NULL')
         max_odds = cur.fetchone()[0]
         print(f'With max_home_odds: {max_odds} ({100*max_odds/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE odds_asian_handicap IS NOT NULL')
         ah = cur.fetchone()[0]
         print(f'With odds_asian_handicap: {ah} ({100*ah/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE odds_over_under_25 IS NOT NULL')
         ou = cur.fetchone()[0]
         print(f'With odds_over_under_25: {ou} ({100*ou/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE opening_avg_home_odds IS NOT NULL')
         o_avg = cur.fetchone()[0]
         print(f'With opening_avg_home_odds: {o_avg} ({100*o_avg/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE opening_max_home_odds IS NOT NULL')
         o_max = cur.fetchone()[0]
         print(f'With opening_max_home_odds: {o_max} ({100*o_max/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE closing_avg_home_odds IS NOT NULL')
         c_avg = cur.fetchone()[0]
         print(f'With closing_avg_home_odds: {c_avg} ({100*c_avg/total:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM matches WHERE closing_max_home_odds IS NOT NULL')
         c_max = cur.fetchone()[0]
         print(f'With closing_max_home_odds: {c_max} ({100*c_max/total:.1f}%)')

         cur.execute('SELECT MIN(match_date), MAX(match_date) FROM matches')
         date_range = cur.fetchone()
         print(f'Date range: {date_range[0]} to {date_range[1]}')

         cur.close()
         conn.close()
         " 2>&1)
     Read(/home/arch/Coding/algobet/alembic/versions/9f6b4c2d8a1e_add_opening_closing_odds_to_matches.py)
     Bash(docker exec algobet-api python3 -c "
         import os, psycopg2
         db_url = os.environ.get('DATABASE_URL') or
         f\"postgresql://{os.environ['POSTGRES_USER']}:{os.environ['POSTGRES_PASSWORD']}@{os.environ['POSTGRES_HOST']}:{os.environ['POSTGRES_PORT']}/{os.environ['POSTGRES_DB']}\"
         conn = psycopg2.connect(db_url)
         cur = conn.cursor()

         # Season breakdown
         cur.execute('SELECT season_id, COUNT(*) FROM matches GROUP BY season_id ORDER BY season_id')
         rows = cur.fetchall()
         print('Matches per season_id:')
         for r in rows:
             print(f'  season_id={r[0]}: {r[1]}')

         # Match statistics
         cur.execute('SELECT COUNT(*) FROM match_statistics')
         total_stats = cur.fetchone()[0]
         print(f'\nTotal match_statistics rows: {total_stats}')
         cur.execute('SELECT COUNT(*) FROM matches WHERE home_score IS NOT NULL AND away_score IS NOT NULL')
         completed = cur.fetchone()[0]
         print(f'Coverage of completed matches: {100*total_stats/completed:.1f}%')

         cur.execute('SELECT COUNT(*) FROM match_statistics WHERE home_xg IS NOT NULL')
         xg = cur.fetchone()[0]
         print(f'With home_xg: {xg} ({100*xg/total_stats:.1f}% of stats rows)')

         cur.execute('SELECT COUNT(*) FROM match_statistics WHERE home_ppda IS NOT NULL')
         ppda = cur.fetchone()[0]
         print(f'With home_ppda: {ppda} ({100*ppda/total_stats:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM match_statistics WHERE home_shots IS NOT NULL')
         shots = cur.fetchone()[0]
         print(f'With home_shots: {shots} ({100*shots/total_stats:.1f}%)')

         cur.execute('SELECT COUNT(*) FROM match_statistics WHERE home_corners IS NOT NULL')
         corners = cur.fetchone()[0]
         print(f'With home_corners: {corners} ({100*corners/total_stats:.1f}%)')

         # Opening vs closing odds analysis
         cur.execute('''
         SELECT COUNT(*) FROM matches
         WHERE opening_odds_home IS NOT NULL AND closing_odds_home IS NOT NULL
         ''')
         both = cur.fetchone()[0]
         print(f'\nMatches with both opening AND closing odds: {both}')

         cur.execute('''
         SELECT COUNT(*) FROM matches
         WHERE opening_odds_home IS NOT NULL AND closing_odds_home IS NOT NULL
         AND ABS(opening_odds_home - closing_odds_home) > 0.001
         ''')
         moved = cur.fetchone()[0]
         print(f'Of those, with actual home odds movement: {moved} ({100*moved/both:.1f}%)')

         # CLV distribution
         cur.execute('''
         SELECT
           MIN(opening_odds_home/closing_odds_home - 1) as min_clv,
           MAX(opening_odds_home/closing_odds_home - 1) as max_clv,
           AVG(opening_odds_home/closing_odds_home - 1) as mean_clv,
           STDDEV(opening_odds_home/closing_odds_home - 1) as std_clv,
           COUNT(*) FILTER(WHERE opening_odds_home/closing_odds_home - 1 > 0) as pct_positive
         FROM matches
         WHERE opening_odds_home IS NOT NULL AND closing_odds_home IS NOT NULL AND closing_odds_home > 0
         ''')
         clv = cur.fetchone()
         print(f'CLV home (opening/closing - 1): min={clv[0]:.4f} max={clv[1]:.4f} mean={clv[2]:.4f} std={clv[3]:.4f} pct_positive={100*clv[4]/both:.1f}%')

         cur.close()
         conn.close()
         " 2>&1)

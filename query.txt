no matches found: \n      import os\n      import psycopg2\n      db_url = os.environ.get('DATABASE_URL') or\n      fpostgresql://{os.environ[POSTGRES_USER]}:{os.environ[POSTGRES_PASSWORD]}@{os.environ[POSTGRES_HOST]}:{os.environ[POSTGRES_PORT]}/{os.environ[POSTGRES_DB]}\n      conn = psycopg2.connect(db_url)\n      cur = conn.cursor()\n\n      print('=== FINAL RESULTS DISTRIBUTION (opening_odds vs result) ===')\n      # Result categories: 1=Home win, 2=Draw, 3=Away win\n      cur.execute('''\n          SELECT\n              COUNT(*) as n,\n              SUM(CASE WHEN home_score > away_score THEN 1.0 ELSE 0.0 END) as n_home,\n              SUM(CASE WHEN home_score = away_score THEN 1.0 ELSE 0.0 END) as n_draw,\n              SUM(CASE WHEN home_score < away_score THEN 1.0 ELSE 0.0 END) as n_away,\n              ROUND(AVG(home_score > away_score)::numeric, 4) as pct_home,\n              ROUND(AVG(home_score = away_score)::numeric, 4) as pct_draw,\n              ROUND(AVG(home_score < away_score)::numeric, 4) as pct_away\n          FROM matches\n          WHERE opening_odds_home IS NOT NULL AND opening_odds_draw IS NOT NULL AND opening_odds_away IS NOT NULL\n            AND home_score IS NOT NULL AND away_score IS NOT NULL\n      ''')\n      r = cur.fetchone()\n      print(f'Total matches for this comparison: {r[0]}')\n      print(f'  Home wins:  {int(r[1])} ({r[5]:.2%})')\n      print(f'  Draws:      {int(r[2])} ({r[6]:.2%})')\n      print(f'  Away wins:  {int(r[3])} ({r[7]:.2%})')\n      print(f'Expected result distribution (implied probs): P(home)={1/r[4]:.4f}, P(draw)={1/r[5]:.4f}, P(away)={1/r[6]:.4f}')\n      print()\n\n      cur.execute('''\n          SELECT\n              opening_odds_home,\n              opening_odds_draw,\n              opening_odds_away,\n              home_score,\n              away_score,\n              CASE\n                  WHEN home_score > away_score THEN 1\n                  WHEN home_score = away_score THEN 2\n                  ELSE 3\n              END as result_cat\n          FROM matches\n          WHERE opening_odds_home IS NOT NULL AND opening_odds_draw IS NOT NULL AND opening_odds_away IS NOT NULL\n            AND home_score IS NOT NULL AND away_score IS NOT NULL\n          ORDER BY opening_odds_home\n          LIMIT 10\n      ''')\n      print('Sample rows: opening odds vs result')\n      for row in cur.fetchall():\n          print(f'  {row[0]:6.2f} {row[1]:6.2f} {row[2]:6.2f} ->  {row[3]}-{row[4]} (result={row[5]})')\n\n      conn.close()\n